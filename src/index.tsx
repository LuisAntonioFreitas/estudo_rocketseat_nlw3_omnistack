import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// JSX - JavaScript XML
// TSX - TypeScript XML

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
